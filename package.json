{
  "name": "living-detective",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Procedural detective simulation — every clue emerges from a real simulated event.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
